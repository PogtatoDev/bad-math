#pragma once
#include "constants.hpp"

namespace Trig
{
    real sin(real x);
    real cos(real x);
    real tan(real x);
    real sec(real x);
    real csc(real x);
    real cot(real x);
    real alt_sin(real x);
    real alt_cos(real x);
}